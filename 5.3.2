elements = [int(n) for n in input("Enter a list of distinct elements (separated by spaces): ").split()]
total = 0
for i in range(len(elements)):
subset = [elements[i]]
  for j in range(i + 1, len(elements)):
    subset.append(elements[j])
    total += 1
print("The total number of subsets is:", total)
